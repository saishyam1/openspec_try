[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "currency-converter"
version = "0.1.0"
description = "Simple currency converter FastAPI app"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "pydantic>=2.5",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
